---
name: gh-cli
description: Reference the upstream GitHub gh CLI skill for authenticated GitHub workflows in deployments that propagate GITHUB_TOKEN to agents.
---

Reference the upstream skill at `https://skills.sh/github/awesome-copilot/gh-cli`.

Use this skill whenever an authenticated deployment provides `GITHUB_TOKEN` and the agent should operate on GitHub through the `gh` CLI instead of Paperclip GitHub agent tools.
